'use client'

import Wheel from './wheel'

export default function ValueProposition() {
  return (
    <section id='value-proposition' className='relative flex h-175 w-full items-center justify-center overflow-hidden'>
      <Wheel />
    </section>
  )
}